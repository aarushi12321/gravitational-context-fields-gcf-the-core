# GCF Lab

This repo contains command-line experiments for the GCF engine.

## Run

From the workspace root:

```bash
npm run demo
```

The demo feeds a scripted conversation into the field and prints the hottest chunks after each turn, including gravity and tier.

## Purpose

The lab is useful for tuning decay, momentum behavior, and migration thresholds without opening the browser app.
